name: ascendia
description: "Ascendia core: Auth, Premium, Feed, Guilds, Chat."
publish_to: 'none'
version: 1.2.0+3

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  # Firebase
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.1
  cloud_firestore: ^5.4.4
  # RevenueCat
  purchases_flutter: ^6.26.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/medals/
    - assets/avatars/
